import { MinimumOnePipe } from './minimum-one.pipe';

describe('MinimumOnePipe', () => {
  it('create an instance', () => {
    const pipe = new MinimumOnePipe();
    expect(pipe).toBeTruthy();
  });
});
