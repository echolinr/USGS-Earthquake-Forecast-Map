import { PercentageForecastPipe } from './percentage-forecast.pipe';

describe('PercentageForecastPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageForecastPipe();
    expect(pipe).toBeTruthy();
  });
});
