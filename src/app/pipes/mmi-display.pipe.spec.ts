import { MmiDisplayPipe } from './mmi-display.pipe';

describe('MmiDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MmiDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
